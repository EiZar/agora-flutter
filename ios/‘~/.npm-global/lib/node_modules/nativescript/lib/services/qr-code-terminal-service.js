"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.QrCodeTerminalService = void 0;
const qrcode = require("qrcode-terminal");
const yok_1 = require("../common/yok");
class QrCodeTerminalService {
    constructor($logger) {
        this.$logger = $logger;
    }
    generate(url) {
        try {
            qrcode.generate(url);
        }
        catch (err) {
            this.$logger.info(`Failed to generate QR code for ${url}`, err);
        }
    }
}
exports.QrCodeTerminalService = QrCodeTerminalService;
yok_1.injector.register("qrCodeTerminalService", QrCodeTerminalService);
